/// The Slots feature's public API: its screens. Nothing else in the app may
/// import `features/slots/…` directly (spec 8.3).
library;

export 'ui/slot_detail_page.dart';
export 'ui/slots_page.dart';
